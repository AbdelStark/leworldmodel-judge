# SCHEDULE

## M0 — spec package
Seed all spec + RFC files.

## M1 — benchmark contract lock
Choose environment, task IDs, labels, metrics, baselines.

## M2 — data + baseline layer
Collect trajectories, compute sparse-reward and heuristic baselines.

## M3 — first judge signal
Implement one world-model-derived score.

## M4 — benchmark + demo
Produce one table, one plot, one replay surface.
